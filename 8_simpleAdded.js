export const simpleAdded = (num) => {
    let sum = 0;

    for (let i = 0; i <= num; i++) {
        sum +=i;

    }
    return sum;
}
//söylenilen sayı dahil o sayıya kadar olan sayıların toplamını verir.